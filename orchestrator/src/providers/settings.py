"""Единые параметры генерации и ключи. Главный файл всей затеи со сравнением.

Если у одной модели температура выше, а у другой лимит короче — сравниваются
настройки, а не модели. Поэтому параметры лежат в одном месте, одним объектом,
и раздаются всем игрокам без исключений. Всё, что выставлено, уходит в отчёт;
всё, что выставить не удалось, уходит туда же отдельным списком.

Ключи берутся только из окружения. Ни в конфиг, ни в лог, ни в отчёт они не
попадают: наружу отдаётся длина и последние четыре знака, этого хватает, чтобы
отличить один ключ от другого, и не хватает, чтобы им воспользоваться.
"""

from __future__ import annotations

import os
from dataclasses import asdict, dataclass, field

# Провайдер → переменная окружения с ключом. Claude идёт по подписке, ключа не
# просит: у него в этой таблице пусто, и проверка его пропускает.
KEYS: dict[str, str | None] = {
    "claude": None,
    "stub": None,
    "stub": None,
    "openai": "OPENAI_API_KEY",
    "gemini": "GEMINI_API_KEY",
    "xai": "XAI_API_KEY",
    "openrouter": "OPENROUTER_API_KEY",
}

# Уровень размышления в терминах каждого вендора. Выключить у всех нельзя:
# Gemini и Grok этого не умеют, минимум у обоих — «low». Значит выравниваемся по
# нижней общей ступени. Одно и то же слово у разных вендоров означает разное
# число токенов — это записано в РАСХОЖДЕНИЯ и уходит в отчёт.
REASONING: dict[str, dict[str, str]] = {
    "claude": {"выкл": "low", "low": "low", "medium": "medium", "high": "high"},
    "openai": {"выкл": "none", "low": "low", "medium": "medium", "high": "high"},
    "gemini": {"выкл": "low", "low": "low", "medium": "medium", "high": "high"},
    "xai": {"выкл": "low", "low": "low", "medium": "medium", "high": "high"},
    # Qwen 3.7 Max размышление поддерживает, и прослойка его пробрасывает:
    # «reasoning» есть в supported_parameters эндпоинта.
    "openrouter": {"выкл": "low", "low": "low", "medium": "medium", "high": "high"},
}

# Что заведомо расходится между вендорами. Список ведётся руками и намеренно:
# это не отговорки, а те самые «известные расхождения», без которых блок
# параметров в отчёте был бы враньём. Проверка возможностей (проверка.py)
# дописывает сюда то, что выяснила живым запросом.
DISCREPANCIES: dict[str, list[str]] = {
    "claude": [
        "температура, top_p и лимит ответа подписочным Agent SDK не задаются: "
        "в ClaudeAgentOptions таких полей нет. Идут вендорские умолчания "
        "(температура 1.0). Лимит ответа не задан, поэтому обрыв на полуслове у "
        "Claude в принципе невозможен — у остальных троих он задан с запасом и "
        "по логу видно, сколько раз сработал.",
        "стоимость приходит от SDK готовой суммой (total_cost_usd) и по подписке "
        "не списывается — это пересчёт, а не счёт.",
        "токены размышления SDK отдельной строкой не отдаёт: они входят в "
        "output_tokens и не выделяются. Доля размышления у Claude поэтому "
        "показана нулём — это «неизвестно», а не «не думал».",
        "уровень размышления выравнивается только у игрока. Мастер и судья вне "
        "сравнения и идут на вендорском умолчании: срезать им размышление до "
        "общей нижней ступени значило бы испортить сессию ради паритета, "
        "который к ним не относится.",
    ],
    "openai": [
        "max_output_tokens считает вместе с токенами размышления, а не только "
        "видимый текст. Замерено на круге ноль: из лимита ушло на размышление "
        "40%.",
    ],
    "gemini": [
        "thinking_level выключить нельзя, нижняя ступень у 3.1 Pro — «low».",
        "вендор прямо просит держать температуру на умолчании 1.0 и предупреждает, "
        "что меньшие значения ведут к зацикливанию. Мы её и держим на 1.0, так что "
        "расхождения здесь нет — но выставить общую температуру 0.7 было бы нельзя.",
    ],
    "openrouter": [
        "единственный из четверых идёт через прослойку, а не напрямую. Апстрим "
        "закреплён явно и фолбэк запрещён, иначе маршрутизатор вправе сменить "
        "хост посреди прогона. Кто отвечал на самом деле — в каждом ходе лога.",
        "автоматического кэширования по префиксу у этой модели нет "
        "(supports_implicit_caching: false), в отличие от GPT и Grok. Кэш только "
        "явный, по пометкам на системном промпте. Если доля попаданий нулевая — "
        "это не сбой, а разница в условиях, и её видно в таблице расхода.",
        "квантизация сборки у апстрима не объявлена (quantization: unknown), "
        "то есть точную сборку мы закрепить не можем — только хост.",
        "деньги берутся из ответа самой прослойки (usage.cost), а не пересчётом "
        "по ставкам: это её собственный счёт и он точнее.",
        "размышляет несопоставимо больше остальных при том же уровне «low»: на "
        "круге ноль 1890 токенов из 2002, то есть 94% лимита против 40% у GPT и "
        "66% у Grok. Слово «low» у вендоров означает очень разное, и лимит "
        "ответа пришлось поднять до 8000 ради одного этого игрока.",
        "самый медленный из четверых: 45 с против 5–11 с у остальных.",
    ],
    "xai": [
        "reasoning_effort выключить нельзя, нижняя ступень — «low».",
        "max_tokens считает вместе с размышлением. Замерено на круге ноль: 66% "
        "лимита ушло на размышление.",
        "запрос длиннее 200 тыс. токенов дорожает целиком, а не в части превышения. "
        "Контекст игрока к концу сессии около 67 тыс., порог не задевается.",
    ],
}


@dataclass(frozen=True)
class GenerationParams:
    """Одно и то же у всех четырёх игроков. Меняется только целиком."""

    # 1.0 — не произвольный выбор, а единственное значение, которое одновременно
    # является умолчанием у всех четырёх вендоров и рекомендованным режимом
    # Gemini. Любое другое пришлось бы выставлять троим и не выставлять Claude.
    temperature: float | None = 1.0
    top_p: float | None = None          # не задаём никому: у Claude не задаётся
    # Промпт просит ≤5 предложений, то есть сотни две токенов текста. Остальное —
    # запас под размышление: лимит считает его вместе с видимым текстом.
    #
    # Число выбрано по замерам, а не на глаз. Qwen на круге ноль: 692 токена
    # размышления при входе 81 и 2896 при входе 4258. К тридцатому кругу вход
    # дорастёт до ~67 тыс. — ещё в шестнадцать раз, — и даже при затухающем
    # росте 8000 может не хватить. Неиспользованный запас не стоит ничего:
    # платят за выданные токены, а не за потолок. Обрыв на полуслове стоит хода.
    max_output: int = 16000
    reasoning: str = "low"            # ключ в таблице РАЗМЫШЛЕНИЕ
    timeout_s: float = 300.0            # длинный ход бывает медленным, рубить рано нельзя
    attempts: int = 4                    # только на сеть и частотные лимиты, не на отказы
    pause_s: float = 2.0                # первая пауза, дальше удвоение
    limit_pause_s: float = 60.0

    def level(self, provider: str) -> str | None:
        """Уровень размышления словом этого вендора."""
        table = REASONING.get(provider)
        return table.get(self.reasoning) if table else None

    def mapping(self) -> dict:
        return asdict(self)


@dataclass
class KeyReport:
    found: dict[str, str] = field(default_factory=dict)   # провайдер → хвост ключа
    without_key: list[str] = field(default_factory=list)        # провайдеры на подписке


def _tail(key: str) -> str:
    """Опознавательный знак ключа, по которому его нельзя восстановить."""
    return f"…{key[-4:]} ({len(key)} знаков)"


def check_keys(providers: set[str] | list[str]) -> KeyReport:
    """Падает при старте, если ключа нет. Падать на двадцатом круге — худшее.

    Проверяются только те провайдеры, которые реально заняты в этом прогоне:
    дым-тест на одном вендоре не должен требовать ключей от трёх остальных.
    """
    report = KeyReport()
    none: list[str] = []

    for name in sorted(set(providers)):
        if name not in KEYS:
            raise SystemExit(
                f"провайдер {name!r} не знает, откуда брать ключ; "
                f"известны: {', '.join(sorted(KEYS))}"
            )
        var = KEYS[name]
        if var is None:
            report.without_key.append(name)
            continue
        value = (os.environ.get(var) or "").strip()
        if not value:
            none.append(f"  {name:8} → {var}")
        else:
            report.found[name] = _tail(value)

    if none:
        raise SystemExit(
            "не хватает ключей в окружении:\n"
            + "\n".join(none)
            + "\n\nПоложить их в файл только для ключей и подтянуть перед прогоном:\n"
            "  touch ~/.нри-ключи && chmod 600 ~/.нри-ключи\n"
            "  echo 'export OPENAI_API_KEY=\"...\"' >> ~/.нри-ключи\n"
            "  source ~/.нри-ключи\n"
            "В конфиг ключи не класть: он копируется в папку прогона и уходит в репозиторий."
        )
    return report


def key(provider: str) -> str:
    """Ключ провайдера. Наличие уже проверено при старте, здесь только выдача."""
    var = KEYS.get(provider)
    if var is None:
        raise RuntimeError(f"провайдеру {provider!r} ключ не положен")
    value = (os.environ.get(var) or "").strip()
    if not value:
        raise RuntimeError(f"{var} пуста, а проверка при старте это пропустила")
    return value


def block_params(
    params: GenerationParams,
    providers: dict[str, str],
    observed_discrepancies: dict[str, list[str]] | None = None,
) -> dict:
    """Блок «условия равные» для лога и отчёта.

    `провайдеры` — кто за кем: персонаж → провайдер. Расхождения складываются из
    заранее известных и тех, что выяснила проверка возможностей живым запросом.
    """
    taken = sorted(set(providers.values()))
    discrepancies: dict[str, list[str]] = {}
    for name in taken:
        lines = list(DISCREPANCIES.get(name, []))
        lines += list((observed_discrepancies or {}).get(name, []))
        if lines:
            discrepancies[name] = lines

    return {
        "given_everyone": {
            "temperature": params.temperature,
            "top_p": params.top_p,
            "max_output_tokens": params.max_output,
            "reasoning": params.reasoning,
            "timeout_s": params.timeout_s,
            "attempts": params.attempts,
        },
        "размышление_словами_вендора": {
            name: params.level(name) for name in taken
        },
        "cast": providers,
        "known_discrepancies": discrepancies,
        "оговорка_о_детерминизме":
            "Броски воспроизводятся по зерну: то же зерно даёт ту же последовательность. "
            "Реплики моделей не воспроизводятся ни у одного из четырёх вендоров, даже при "
            "тех же параметрах и том же контексте. Повторный запуск даст те же кубики и "
            "другой текст.",
    }
