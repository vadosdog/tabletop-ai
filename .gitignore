# Виртуальное окружение с SDK — 328 МБ, ставится одной командой из README
.venv/

# Питоновский мусор
__pycache__/
*.py[cod]

# Локальные разрешения Claude Code — у каждого свои
.claude/settings.local.json
